version: "3.8"

services:
  app:
    build: .
    container_name: daihuo-jianshou
    restart: unless-stopped
    ports:
      - "80:3000"
    volumes:
      - sqlite_data:/app/data
    environment:
      - NODE_ENV=production

volumes:
  sqlite_data:
